// PACKED CARTON

function readURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image").attr("src", e.target.result);
            $(".file-upload-content").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END PACKED CARTON

// PACKED INTO CARTON

function readURL2(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image2").attr("src", e.target.result);
            $(".file-upload-content2").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END PACKED INTO CARTON

// SILICA GEL

function readURL3(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image3").attr("src", e.target.result);
            $(".file-upload-content3").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END SILICA GEL

// SAMPLING CARTON

function readURL4(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image4").attr("src", e.target.result);
            $(".file-upload-content4").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END SAMPLING CARTON

// PACKING LIST

function readURL5(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image5").attr("src", e.target.result);
            $(".file-upload-content5").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END PACKING LIST

// HANG TAG

function readURL6(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image6").attr("src", e.target.result);
            $(".file-upload-content6").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END HANG TAG

// SHIPPING MARK

function readURL7(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image7").attr("src", e.target.result);
            $(".file-upload-content7").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END SHIPPING MARK

// POLYBAG PACKED

function readURL8(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image8").attr("src", e.target.result);
            $(".file-upload-content8").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END POLYBAG PACKED

// MAIN LABEL

function readURL9(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image9").attr("src", e.target.result);
            $(".file-upload-content9").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END MAIN LABEL

// CARE LABEL FRONT

function readURL10(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image10").attr("src", e.target.result);
            $(".file-upload-content10").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END CARE LABEL FRONT

// CARE LABEL BACK

function readURL11(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image11").attr("src", e.target.result);
            $(".file-upload-content11").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END CARE LABEL BACK

//=====  LAB ANALYSIS & TESTING  ======//

// IMAGE 1

function readURL12(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image12").attr("src", e.target.result);
            $(".file-upload-content12").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END IMAGE 1

//=====  PRODUCT  ======//

// PRODUCT VIEW

function readURL13(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image13").attr("src", e.target.result);
            $(".file-upload-content13").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END PRODUCT VIEW

// SHADING VIEW

function readURL14(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image14").attr("src", e.target.result);
            $(".file-upload-content14").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END SHADING VIEW

// COMPARE SAMPLE

function readURL15(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image15").attr("src", e.target.result);
            $(".file-upload-content15").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END COMPARE SAMPLE

//=====  MEASURMENTS  ======//

// MEASUREMENT 1

function readURL16(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image16").attr("src", e.target.result);
            $(".file-upload-content16").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END MEASUREMENT 1

// MEASUREMENT 2

function readURL17(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image17").attr("src", e.target.result);
            $(".file-upload-content17").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END MEASUREMENT 2

// MEASUREMENT 3

function readURL18(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image18").attr("src", e.target.result);
            $(".file-upload-content18").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END MEASUREMENT 3

// MEASUREMENT 4

function readURL19(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image19").attr("src", e.target.result);
            $(".file-upload-content19").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END MEASUREMENT 4

// MEASUREMENT 5

function readURL20(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function (e) {
            $(".file-upload-image20").attr("src", e.target.result);
            $(".file-upload-content20").show();
        };

        reader.readAsDataURL(input.files[0]);
    }
}

// END MEASUREMENT 5
